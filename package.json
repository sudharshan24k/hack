{
  "name": "portfolio-management-api",
  "version": "1.0.0",
  "description": "A portfolio management API built with Node.js, Express, and Mongoose",
  "main": "src/server.js",
  "scripts": {
    "kill-ports": "node scripts/kill-ports.js",
    "prestart": "npm run kill-ports && sleep 1",
    "start": "node --no-deprecation src/server.js",
    "dev": "nodemon --no-deprecation src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "portfolio",
    "api",
    "express",
    "mongoose"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.0.3",
    "react-icons": "^5.5.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
